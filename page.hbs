{{!< default}}
<main>
    <article class="{{post_class}}">
        {{#post}}
            <header>
                <h1>{{{title}}}</h1>
            </header>
            <section>
                {{content}}
            </section>
        {{/post}}
    </article>
</main>
